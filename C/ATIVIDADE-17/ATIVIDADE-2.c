/*02. Faça um programa em C que leia um número e após a leitura, calcule e imprima a raiz quadrada deste número.
*/

#include <stdio.h>
#include <math.h>
int main()
{
    float num,raiz;

    printf("Digite um valor para ver a seu raiz: ");
    scanf("%f",&num);

    raiz = sqrt(num);
    printf("A raiz quadrada de %d é %d", num, raiz)

    printf("A area do retangulo e %.2f",area(altura,base));

}
